{
  "name": "super-galactic-age-calculator",
  "version": "1.0.0",
  "description": "A calculator to determine a user's age based on different solar years.",
  "main": "gulpfile.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "browserify": "^15.0.0",
    "gulp": "^3.9.1",
    "gulp-concat": "^2.6.1",
    "gulp-uglify": "^3.0.0",
    "vinyl-source-stream": "^2.0.0"
  }
}
